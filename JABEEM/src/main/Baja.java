package main;

public class Baja extends Operation {
	/*Baja recieves an object of the User type, and deletes it, obviously this needs the invocation of a SEARCH method, 
		that either is invoked here or is invoked to obtain the user, need to think how to do this. either/or ideally this is 
		auctioned off to a db manager and, instead, we use the Unique ID to let the db do its thing
	*/
	
	public void Opera (int ident) {
				
		User usuario = 
		
		System.out.println ("Usuario Eliminado");
	} 
	
}
